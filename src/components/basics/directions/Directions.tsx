'use client';
import React from 'react';
import { Container } from './Directions.styles';

const Directions = () => {
  return (
    <Container>
      <h2>Directions</h2>
    </Container>
  );
};

export default Directions;
