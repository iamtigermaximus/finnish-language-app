'use client';
import React from 'react';
import { Container } from './NumberColors.styles';

const NumbersColors = () => {
  return (
    <Container>
      <h2>NumbersColors</h2>
    </Container>
  );
};

export default NumbersColors;
